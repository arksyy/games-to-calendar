# games-to-calendar

Saison configurée : **Adulte | Automne/Hiver 2026-2027**.

- `./run.sh` : matchs de L'Antichambre en B2 (Interligue).
- `./run.sh gab` : matchs de L’Antichambre en F6+ (Saint-Aug).

Les filtres sont définis dans `.env` ; `.env.example` contient les mêmes valeurs par défaut.
